<!DOCTYPE html>
<html>
  <head>
    <title>ShiftTableVer7</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS (CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <ul>
        <p>管理者ログイン：<%= admin_signed_in? %></p>
        <li><%= link_to "ホーム", root_path %></li>
        <li><%= link_to "週一覧", weeks_path %></li>
        <li><%= link_to "管理者作成", new_admin_registration_path %></li>
        <li><%= link_to "管理者ログイン", new_admin_session_path %></li>
        <li><%= link_to "ログアウト", destroy_admin_session_path, data: {turbo_method: :delete} %></li>
      </ul>
    </header>
    <main>
      <%= yield %>
    </main>
    <footer>

    </footer>
    <!-- Bootstrap JS (CDN) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
